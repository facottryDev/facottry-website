import React from 'react'

type Props = {}

const Filters = (props: Props) => {
  return (
    <div className="mx-10 rounded-lg mb-8 border p-6 max-w-screen-xl">
        Filters
    </div>
  )
}

export default Filters